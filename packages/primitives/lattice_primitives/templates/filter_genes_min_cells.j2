{% block user %}Removed genes detected in fewer than {{ params.get('min_cells_per_gene', 3) }} cells; {{ n_vars }} genes remain after filtering.{% endblock %}

{% block methods %}Lowly-detected genes were removed using sc.pp.filter_genes from Scanpy [scanpy_filter_cells]. Genes expressed in fewer than {{ params.get('min_cells_per_gene', 3) }} cells were excluded to reduce sparsity noise and improve the signal-to-noise ratio in downstream dimensionality reduction and clustering steps.{% endblock %}
