{% block user %}Annotated cell types for all {{ n_obs }} cells with CellTypist model '{{ params.get('model', 'Immune_All_Low.pkl') }}'{% if params.get('majority_voting', True) %} (majority voting){% endif %}; per-cell labels are in adata.obs['celltypist_labels'].{% endblock %}

{% block methods %}Cell types were assigned with CellTypist (model '{{ params.get('model', 'Immune_All_Low.pkl') }}'{% if params.get('majority_voting', True) %}, majority_voting=True{% endif %}) [dominguez_celltypist_2022]. Expression was log1p-normalized to a fixed target count of 10,000 per cell for prediction, as required by CellTypist, without altering the underlying median-normalized matrix. Predicted labels and per-cell confidence scores were stored in adata.obs.{% endblock %}
