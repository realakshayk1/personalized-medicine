{% block user %}Drew a volcano plot of marker genes for group '{{ params.get('group') if params.get('group') is not none else 'the first group' }}' (log2 fold change vs. -log10 adjusted p-value).{% endblock %}

{% block methods %}Differential expression for the selected group was visualized as a volcano plot of log2 fold change against -log10 adjusted p-value, derived from the sc.tl.rank_genes_groups one-vs-rest result [scanpy_rank_genes_groups]. Highlight thresholds were |log2FC| >= {{ params.get('lfc_threshold', 1.0) }} and adjusted p < {{ params.get('pval_threshold', 0.05) }} for display only.{% endblock %}
