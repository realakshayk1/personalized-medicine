{% block user %}Drew a dotplot of the top {{ params.get('n_genes', 5) }} marker genes per group in adata.obs['{{ params.get('groupby', 'leiden') }}'].{% endblock %}

{% block methods %}The top {{ params.get('n_genes', 5) }} ranked marker genes per group were visualized as a dotplot with sc.pl.dotplot from Scanpy (groupby='{{ params.get('groupby', 'leiden') }}'), where dot size encodes the fraction of expressing cells and color encodes mean expression [scanpy_rank_genes_groups].{% endblock %}
